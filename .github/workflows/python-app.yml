name: send_notice_cron

on: [push]
#   schedule:
#   - cron: "0 0 * * *"

jobs:
  job:
    runs-on: ubuntu-latest

    steps:
        - uses: actions/checkout@v3
          with:
            ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
            persist-credentials: true
            
#         - name: Commit files
#           run: |
#             ...
            
        - name: Commit files
          run: |
            git config --local user.email "bongudev@gmail.com"
            git config --local user.name "bong-u"
            git commit -m "RECORD FILE UPDATED" -a
        
        - name: Push changes
          uses: ad-m/github-push-action@master
          with:
            ssh: true
            branch: ${{ github.ref }}
