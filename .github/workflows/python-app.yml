name: Show GSPREAD_AUTH
on: [push]

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - name: Set up secret file
        run: |
          echo ${{secrets.GSPREAD_AUTH}} | sed 's/./& /g'
