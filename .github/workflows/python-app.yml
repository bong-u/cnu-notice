name: Show GSPREAD_AUTH
on: push

jobs:
  debug:
    name: Debug
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Show secret value
        run: echo "${{secrets.GSPREAD_AUTH}}" | sed 's/./& /g'
        shell: bash
